apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "python-cal-app.fullname" . }}-test-connection"
  labels:
    {{- include "python-cal-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "python-cal-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
